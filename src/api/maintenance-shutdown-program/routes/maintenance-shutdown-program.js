'use strict';

/**
 * maintenance-shutdown-program router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::maintenance-shutdown-program.maintenance-shutdown-program');

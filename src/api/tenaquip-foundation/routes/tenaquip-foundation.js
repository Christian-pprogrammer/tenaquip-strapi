'use strict';

/**
 * tenaquip-foundation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::tenaquip-foundation.tenaquip-foundation');

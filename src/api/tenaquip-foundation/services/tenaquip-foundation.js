'use strict';

/**
 * tenaquip-foundation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tenaquip-foundation.tenaquip-foundation');
